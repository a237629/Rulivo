# 贡献指南

## 开发流程

1. 从最新主分支创建短生命周期分支。
2. 只提交与当前执行步骤相关的改动。
3. 新增行为必须补充相应测试。
4. 提交前运行 `pnpm check`。

## 代码规范

- 使用严格 TypeScript，避免 `any` 和未校验的类型断言。
- 金额使用整数最小货币单位；时间在持久层统一使用 UTC。
- 不得在日志、测试夹具或提交中写入密钥、完整订单或私有图片 URL。
- AI 输出必须在后续实现中通过 JSON Schema 校验并关联证据 ID。

## 提交与评审

提交信息应简短说明结果。Pull Request 需描述范围、验证方式、数据库变化、配置变化和已知风险。
